/**
    Simulate the multi-agent warehouse
    @param numOfAgents Number of agents to be simulated
    @param displayEnvironment If the environment is to be displayed 
    @param msSleepDuration Number of milliseconds to sleep between every print of the environment 
    @returns Boolean indicating whether the agents completed the task without collision 

*/

bool simulate(int numOfAgents, bool displayEnvironment, int msSleepDuration);